<Window x:Class="WpfClient.PublishersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfClient"
        xmlns:viewModels="clr-namespace:WpfClient.ViewModels"
        mc:Ignorable="d"
        Title="PublishersWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewModels:PublishersWindowViewModel></viewModels:PublishersWindowViewModel>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <ListBox 
            ItemsSource="{Binding Books}"
            SelectedItem="{Binding SelectedBook}"
            SelectedIndex="{Binding SelectedBookIndex}"
            Grid.Column="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>

                        <Label Grid.Column="0" Content="{Binding b_id}"></Label>
                        <Label Grid.Column="1" Content="{Binding b_title}"></Label>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Column="1">
            <Label Content="Name:" Margin="0"/>
            <TextBox Text="{Binding SelectedBook.b_title}" Margin="10"></TextBox>
            <Label Content="Address:" Margin="0"/>
            <TextBox Text="{Binding SelectedBook.b_author}" Margin="10"></TextBox>
            <Label Content="Website:" Margin="0"/>
            <TextBox Text="{Binding SelectedBook.b_author}" Margin="10"></TextBox>
            <Label Content="Email:" Margin="0"/>
            <TextBox Text="{Binding SelectedBook.b_price}" Margin="10"></TextBox>
            <Label Content="" Margin="0"/>
            <Button Command="{Binding AddBookCommand}" Content="Add Publisher" Margin="10" />
            <Button Command="{Binding EditBookCommand}" Content="Edit Publisher" Margin="10" />
            <Button Command="{Binding DeleteBookCommand}" Content="Delete Publisher" Margin="10" />
        </StackPanel>
    </Grid>
</Window>
